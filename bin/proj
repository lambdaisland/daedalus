#!bin/bb

(ns proj
  (:require [clojure.java.shell :as sh]
            [lioss.main :as lioss]
            [lioss.shellutil :as shellutil]))

(lioss/main
 {:license        :mpl
  :inception-year 2021
  :description    "Path finding and Delaunay triangulation in 2D, cljs wrapper for hxdaedalus-js"})

;; Local Variables:
;; mode:clojure
;; End:
